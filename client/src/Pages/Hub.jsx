const Hub = () => {
  const styles = {
    title: {
      fontSize: "2em",
      color: "White",
      textAlign: "center",
      margin: "0.5em",
    },
  };

  return <div style={styles.title}>The Hub</div>;
};

export default Hub;
